{
  "mcpServers": {
    "fileExplorer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem"],
      "capabilities": {
        "filesystem": ["/", "/home", "/etc", "/var/log"]
      }
    }
  }
}
