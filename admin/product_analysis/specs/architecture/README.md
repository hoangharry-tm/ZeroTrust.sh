# specs/architecture/

This folder contains architecture-level reference documents and diagrams. The `diagrams/` subfolder holds visual architecture diagrams (exported as SVG or PNG). The `adr/` subfolder holds Architecture Decision Records — formal records of significant technical decisions, written only after decisions are made. Files prefixed `LEGACY_` are reference documents migrated from the original workspace and provide useful background context for the current proposal work.
